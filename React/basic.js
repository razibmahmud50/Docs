// inline style
style={{borderColor: `${cardBorderColor}` , borderRadius: `${cardBorderRadius}px`}}

//conditional class
className={`btn ${button_styles ? "btn-" + button_styles : ""} ${button_textColor || "" }`}

//if else
{enableModal ? (''):('')}